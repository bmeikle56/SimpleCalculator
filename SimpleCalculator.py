#Simple Calculator

class SimpleCalculator:
    def main():
        print("Welcome to SimpleCalculator\n")
        inp = input("Enter your 4-function calculation: ")
        #interpret(inp)
    
    def add(a, b):
        return a + b

    def subtract(a, b):
        return a - b

    def multiply(a, b):
        return a * b

    def divide(a, b):
        return a / b

    def contains(op):
    #take into account if the first character is an operation
    #this is not allowed, so quit the program and throw an exception
        #for substr in range(0, )
        pass #to avoid error

    def interpret(str):
        #interpret the String
        #match inp:
            #case 
        pass #to avoid error

    main()

